import { Link } from "react-router-dom";
import { courses } from "../data/learning";

export default function CoursesPage() {
  return <div className="courses-page"><section className="page-heading"><div><span className="eyebrow">THE LIBRARY / 03 COURSES</span><h1>Choose your<br /><em>next question.</em></h1><p>Structured paths for a subject that rewards curiosity more than memorization.</p></div><div className="course-count"><strong>03</strong><span>active paths</span></div></section><div className="course-filters"><button className="filter active" type="button">All courses</button><button className="filter" type="button">Foundations</button><button className="filter" type="button">Build</button><button className="filter" type="button">Algorithms</button></div><div className="course-list">{courses.map((course, index) => <Link className={`course-card ${course.accent}`} key={course.id} to={`/courses/${course.id}`}><span className="course-number">0{index + 1}</span><div className="course-card-main"><span className="eyebrow">{course.category}</span><h2>{course.title}</h2><p>{course.description}</p><div className="course-meta"><span>{course.level}</span><span>{course.duration}</span><span>{course.modules.length} modules</span></div></div><div className="course-progress"><div className="ring"><strong>{course.progress}</strong><small>%</small></div><span>in progress</span><b>↗</b></div></Link>)}</div></div>;
}
