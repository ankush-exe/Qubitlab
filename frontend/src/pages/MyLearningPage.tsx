import { Link } from "react-router-dom";
import { courses } from "../data/learning";

export default function MyLearningPage() {
  return <div className="my-learning-page"><section className="page-heading"><div><span className="eyebrow">YOUR SPACE / MY LEARNING</span><h1>Keep your<br /><em>momentum.</em></h1><p>Everything you are learning, in progress, or ready to explore next.</p></div><span className="learning-summary"><strong>42%</strong><small>overall progress</small></span></section><section className="enrolled-list"><div className="section-title"><span className="eyebrow">ENROLLED COURSES</span><h2>Continue where you left off.</h2></div>{courses.map((course) => <article className="enrolled-course" key={course.id}><span className={`course-symbol ${course.accent}`}>ψ</span><div className="enrolled-main"><span className="eyebrow">{course.category}</span><h3>{course.title}</h3><div className="enrolled-progress"><i style={{ width: `${course.progress}%` }} /></div><small>{course.progress}% complete · {course.modules.length - 1} lessons remaining</small></div><Link className="button secondary" to={`/courses/${course.id}/learn`}>Continue <b>↗</b></Link></article>)}</section><section className="sandbox-card"><div className="sandbox-art">⌁</div><div><span className="eyebrow">FREE PLAY</span><h2>Open Quantum Sandbox</h2><p>Build anything. No lesson context, no score, just a blank circuit and a place to test an idea.</p></div><Link className="button primary" to="/courses/fundamentals/learn?tab=lab&sandbox=true">Open sandbox <b>↗</b></Link></section></div>;
}
