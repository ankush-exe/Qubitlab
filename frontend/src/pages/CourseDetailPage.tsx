import { Link, useParams } from "react-router-dom";
import { courses } from "../data/learning";

export default function CourseDetailPage() {
  const { courseId = "fundamentals" } = useParams();
  const course = courses.find((item) => item.id === courseId) ?? courses[0];
  return <div className="course-detail"><Link className="back-link" to="/courses">← All courses</Link><section className={`course-hero ${course.accent}`}><div><span className="eyebrow">{course.category} / COURSE 0{courses.indexOf(course) + 1}</span><h1>{course.title}</h1><p>{course.description}</p><div className="course-meta"><span>{course.level}</span><span>{course.duration}</span><span>Dr. Maya Chen · Instructor</span></div><Link className="button primary" to={`/courses/${course.id}/learn?tab=overview`}>Start course <b>↗</b></Link></div><div className="course-hero-stat"><strong>{course.progress}%</strong><span>your progress</span><div className="metric-bar"><i style={{ width: `${course.progress}%` }} /></div></div></section><div className="course-body"><section><div className="section-title"><span className="eyebrow">THE CURRICULUM</span><h2>Four ideas, one new lens.</h2></div>{course.modules.map((module, index) => <Link className="module-row" key={module} to={`/courses/${course.id}/learn?tab=${index === 0 ? "overview" : "lesson"}&lesson=${index + 1}`}><span className={`module-state ${index < 2 ? "done" : ""}`}>{index < 2 ? "✓" : `0${index + 1}`}</span><span><small>MODULE 0{index + 1}</small><strong>{module}</strong></span><b>{index === 0 ? "completed" : index === 1 ? "continue" : "locked"}</b><i>›</i></Link>)}</section><aside className="course-aside"><div className="aside-card"><span className="eyebrow">NEXT UP</span><h3>{course.modules[1]}</h3><p>18 min · interactive lesson</p><Link className="button primary" to={`/courses/${course.id}/learn?tab=lesson&lesson=2`}>Resume lesson <b>↗</b></Link></div><div className="aside-card quiet"><span className="eyebrow">THE OUTCOME</span><p>By the end, you will be able to explain a qubit without reaching for a classical analogy.</p></div></aside></div></div>;
}
